# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Сборка и проверки

```bash
npm install              # один раз: ставит jsdom, на нём написаны все проверки
python3 build.py         # пересобрать index.html из шаблона и данных
npm test                 # все четыре набора подряд

node tests/basic.js      # запуск, отметки, сохранение, переключение дня
node tests/edge.js       # битые данные, отказ хранилища, гонки, доступность
node tests/progress.js   # автопереход на следующий день
node tests/audio.js      # фоновый сигнал: старт в касании, работа в фоне
```

Итоговые строки всех четырёх наборов разом, без прокрутки вывода:

```bash
for t in basic edge progress audio; do printf '%-9s ' "$t"; node tests/$t.js 2>&1 | grep -E 'провалов:|ошибок:' | tail -1; done
```

**Коду выхода верить нельзя:** `basic`, `edge` и `progress` завершаются нулём даже при провалах, ненулевой код возвращает только `audio`. Результат читается глазами по последней строке: «провалов: N» у `basic`, `edge`, `audio` и «ошибок: N» у `progress`.

## Устройство сборки

`index.html` — сборочный артефакт, **править его руками нельзя**: любая правка перетирается следующим `python3 build.py`. Интерфейс, стили и скрипт живут в `template.html`, содержание — в `data/*.py`.

`build.py` читает `data/program.py` через `ast.literal_eval`, исполняет `data/terms.py` через `exec` и подставляет три плейсхолдера в `template.html`:

- `__DAYS__` — программа по дням из `DAYS`
- `__GLOSS__` — глоссарий из `TERMS`, ключ картинки разворачивается в путь `assets/gym/<ключ>.jpg`, а `ref:<ключ>` — в ссылку на другой термин
- `__IMGKEYS__` — список ключей иллюстраций, собранный из имён файлов в `assets/exercises/`

Тесты читают собранный `index.html`, а не шаблон. Поэтому правка `template.html` или `data/*.py` без пересборки в тестах не видна.

### Формат упражнения в `data/program.py`

Ключи короткие: `n` — название, `l` — строка подходов («4 × 8–12»), `c` — подсказка, `img` — ключ иллюстрации из `assets/exercises/` без суффикса кадра (`_0` старт, `_1` финиш), `rest` — отдых в секундах, `t` — длительность для кардио, `hold` — удержание, `iv` — интервалы `[работа, спокойно, круги]`, `w: 1` — разминочное упражнение.

Упражнение считается силовым (с отметками подходов и полем веса), если у него нет ни `w`, ни `t`, ни `iv` — эта проверка повторяется и в коде, и в тестах.

Внимание на совпадение имён: `w` у упражнения — это признак разминки, а `w` в записи журнала весов — сам вес.

### Формат термина в `data/terms.py`

Кортеж `(ключ, заголовок, объяснение, ключ картинки или None, [шаблоны])`. Шаблоны — регулярные выражения для подсветки термина прямо в тексте карточек; `\w` в них заменяется на класс, включающий кириллицу.

## Правила, выведенные опытом

1. **После любой правки прогонять все четыре набора тестов.** Они уже ловили обращение к переменной до инициализации, падение всего приложения от одной битой записи в журнале и рассинхрон таймера с отметкой подхода.
2. **Тренажёры опознаются только по наклейке на самой машине,** не по внешнему виду — так уже дважды ошиблись. Ориентир — надпись на наклейке (LEG PRESS, LAT MACHINE, ARM CURL, KNEELING EASY CHIN DIP, PECTORAL / REVERSE FLY), а не номер машины. Нет наклейки на фото — просить фото наклейки, не гадать.
3. **Картинки не встраивать в `index.html`.** Когда они лежали в base64, файл весил 2,1 МБ и предпросмотр обрезал его на середине скрипта. Сейчас 89 КБ.
4. **Репозиторий публичный:** на фотографиях зала не должно быть узнаваемых посторонних людей.
5. **Числа и факты программы проверять по авторитетным источникам** перед внесением.

## Ограничения платформы, влияющие на код

Приложение живёт на айфоне как иконка на домашнем экране, оболочка — WebKit. Отсюда неочевидные решения в `template.html`:

- **Основной путь к сигналу — экран остаётся включённым.** Отдых идёт при удержанном `wakeLock`, тип аудиосессии на старте отсчёта выставляется в `transient` и возвращается в `auto` после конца. `transient` звучит поверх чужой музыки и не ставит её на паузу; в фоне он молчит, но это и не требуется — приложение остаётся на экране. Измерено на iOS 26.3, см. `docs/research/audio-session-matrix.md`.
- **Жест обязателен для звука и для блокировки экрана.** WebKit отклоняет `navigator.wakeLock.request()` с `NotAllowedError` вне пользовательского жеста, хотя в спецификации W3C такого требования нет; `AudioContext` вне жеста не поднимается. Из консоли веб-инспектора оба вызова дают ложный отрицательный результат — мерить их можно только из касания.
- **Зависший `AudioContext`.** После ухода в другое приложение и возврата контекст остаётся в состоянии `running`, но `currentTime` стоит на месте и динамик молчит; `resume()` не помогает. Лечится только `close()` и созданием нового контекста, надёжно — внутри касания. Поэтому отсчёт всегда стартует со свежим контекстом (`audioReady(true)` в `startPhases`), а `beep()` распознаёт остановившиеся часы и пересоздаёт контекст сам.
- **Фоновая дорожка — выключенная запасная опция.** WAV «тишина нужной длины плюс сигналы» под переключателем `cfg().bg`, тип сессии для неё жёстко `playback`: только он звучит после ухода в другое приложение, ценой паузы в чужой музыке на всё время отдыха. Запускать дорожку обязательно внутри касания и не останавливать при уходе из вкладки — начать воспроизведение при уже скрытой странице WebKit не даёт, `play()` отклоняется молча. Пока дорожка играет, `beep()` не дублирует её звук. **По умолчанию выключена**, не включать: тишина вместо музыки на каждом отдыхе хуже, чем отсутствие сигнала в фоне.
- **Второй путь к сигналу в фоне** — `fireExternal()`: отдать отсчёт системному таймеру через «Быстрые команды» (`cfg().ext`, тоже выключено по умолчанию). Системный таймер звонит при заблокированном экране и музыку только приглушает.
- **Уведомления.** Web Push для приложений с домашнего экрана на iOS есть начиная с 16.4 — прежняя запись «уведомления недоступны» неверна. Но в проекте нет ни `manifest.json`, ни service worker, ни сервера-планировщика, так что путь пока закрыт технически, а не платформой.
- **Кэш домашнего экрана.** Приложение с иконки держит ранее загруженную страницу. Перед любой проверкой новой сборки на устройстве его надо закрыть из переключателя приложений и открыть заново — иначе тестируется старый код.
- **Браузерная память** недоступна страницам со схемы `file:` — проверять сохранение весов можно только по адресу `https://`.

## Отладка на устройстве

Safari Web Inspector даёт консоль живого приложения, открытого с домашнего экрана. Это единственный способ увидеть настоящие `actx.state`, `navigator.audioSession.type` и причину отказа вместо догадок.

На iPhone: Настройки → Приложения → Safari → Дополнения → Веб-инспектор (на iOS 18 и новее настройки приложений лежат в разделе «Приложения»). Подключить кабелем, разблокировать, ответить «Доверять».

На Mac: Safari → Настройки → Advanced → «Show features for web developers», затем в строке меню Develop → имя iPhone → строка приложения. Приложение должно быть на переднем плане в момент подключения; соединение переживает сворачивание, но обрывается при закрытии приложения из переключателя.

В консоли доступны имена верхнего уровня скрипта: `cfg()`, `actx`, `wake`, `bgAudio`, `bgUrl`, `beep()`, `startTimer()`. Замеры, требующие жеста, оформлять одноразовым обработчиком на `touchend` и `click` в фазе перехвата, иначе результат ложный.

## Публикация

GitHub Pages, репозиторий `YurkaGagarin/fruman-gym`, ветка `main`, папка `/ (root)`. Публикуются `index.html` вместе с папкой `assets` — картинки лежат отдельными файлами.

## Общение

Кратко и по делу: сначала ответ, потом детали. Вопрос «да/нет» — начинать с «да» или «нет». Не пересказывать сделанное и не объяснять назначение вещей без запроса. При сомнении в понимании задачи — уточнять, а не додумывать.
